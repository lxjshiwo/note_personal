		函数的生命周期：被调用创建，返回时终止
			return 命令返回 返回自定义状态结果
				0:成功
				1-255:失败
		函数的变量作用范围
			本地变量:当前shell进程，为了执行脚本会启动专用的shell进程,本地变量的作用范围是shell脚本程序文件
			局部变量：函数上下问，函数的生命周期,函数结束时变量被自动销毁
				如果函数中有同名的局部变量



			
