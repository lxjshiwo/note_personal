安装 Apk
      查看相应的应用清单文件查看那安装
      注意:
      1.包名一致:升级应用只有唯一性质的一个，将原有应用覆盖
      2.签名一致:
        从eclipse运行的是bin目录下的应用，使用debug.keystore签名应用
        签名一致包名不一致将生成两个相应apk

        在.android目录下
            手机卫士版本1为debug.keystore签名
            手机卫士版本2应该也使用相应的签名文件打包

      签名不一致:包名相同，安装失败


    
